# Phase 7 — Deployment

You are an expert in containerizing Python applications with Docker. 
I'm building a lyrics RAG pipeline for my thesis and want to containerize 
the Streamlit frontend for reproducible deployment and thesis demonstration.

My project structure is as follows:

RAG SYSTEM/
├── app/               ← Streamlit frontend (app.py lives here)
├── collection/
│   ├── clean_lyrics.py
│   └── ingest_lyrics.py
├── data/
│   ├── failed/
│   ├── processed/     ← faiss.index, metadata.json, chunks.jsonl
│   └── raw/
├── notebooks/         ← Phase 6 evaluation notebook
├── pipeline/
│   ├── chunking.py
│   ├── embedding.py
│   ├── query.py       ← QueryPipeline class
│   └── reranker.py    ← Reranker class
├── utils/             ← shared helper functions
├── .env               ← API keys (never baked into image)
├── .gitignore
├── charts.csv
├── charts_test.csv
└── requirements.txt

The app requires:
- The FAISS index and metadata (data/processed/) to already exist 
  on disk — these are generated by the pipeline scripts before 
  containerization, not inside the container
- ANTHROPIC_API_KEY passed as an environment variable at runtime
- Optional: OLLAMA_HOST environment variable pointing to a running 
  Ollama instance (e.g., http://host.docker.internal:11434 if Ollama 
  runs on the host machine)

Help me write the following:

1. A Dockerfile that:
   - Uses a slim Python base image (recommend the best version for 
     this stack and explain why)
   - Installs all dependencies from requirements.txt
   - Copies only the files needed to run the Streamlit app at runtime:
     app/, pipeline/, utils/, data/processed/, and requirements.txt
   - Does NOT copy: collection/, notebooks/, data/raw/, data/failed/, 
     charts.csv, charts_test.csv, or .env
   - Exposes port 8501 (Streamlit default)
   - Sets a sensible ENTRYPOINT to launch the Streamlit app from 
     the app/ directory
   - Is optimized for image size — explain any size reduction 
     decisions you make (e.g., --no-cache-dir, .dockerignore, 
     slim base image)

2. A .dockerignore file that excludes:
   - data/raw/ and data/failed/ (large, not needed at runtime)
   - collection/ scripts (not needed at runtime)
   - notebooks/ (not needed at runtime)
   - .env file (secrets must never be baked into the image)
   - charts.csv and charts_test.csv
   - Python cache files (__pycache__, *.pyc)
   - Virtual environments (.venv)
   - .gitignore

3. A docker-compose.yml that:
   - Defines a service for the Streamlit app
   - Passes ANTHROPIC_API_KEY from the host environment into 
     the container (never hardcoded) — load it from the .env 
     file at the project root using the env_file directive
   - Mounts data/processed/ as a volume so the FAISS index can 
     be updated without rebuilding the image
   - Optionally defines an Ollama service using the official 
     ollama/ollama Docker image, with the model volume persisted
   - Maps port 8501 to the host

4. A brief README section (markdown) covering:
   - Prerequisites (Docker, Docker Compose, having run the pipeline 
     scripts first to generate data/processed/)
   - How to build and run with docker-compose up
   - How to pass the API key securely via the .env file at project 
     root (not hardcoded)
   - How to run with Ollama enabled vs. Anthropic only
   - How to rebuild the image after re-embedding (i.e., after 
     re-running pipeline/embedding.py to regenerate data/processed/)

Flag any gotchas specific to this stack — particularly around:
- faiss-cpu compatibility with the chosen base image architecture
- sentence-transformers model caching inside Docker 
  (the HuggingFace cache lives in ~/.cache/huggingface by default — 
  explain whether to bake it into the image or mount it as a volume)
- The host.docker.internal hostname for connecting to Ollama running 
  on the host machine rather than in a container, and whether this 
  works differently on Windows, Mac, and Linux
- Any issues running faiss-cpu or sentence-transformers on a slim 
  base image that may be missing system libraries

If you make assumptions about the host OS, state them and explain 
any differences in the Docker setup across Windows, Mac, and Linux.